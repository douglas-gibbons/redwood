# Redwood

Helper functions for Agents. The storage mechanism is intentionally vague; use prompts to
direct its use.

## Adding to an Agent

Add run.sh as an STDIO MCP server

## Testing with inspector

Run

```
npx @modelcontextprotocol/inspector `pwd`/run.sh
```
