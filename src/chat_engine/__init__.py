"""
Main chat engine logic. 
This is where the main loop lives that takes user input, sends it to the LLM, 
gets the response, and displays it. It also handles function calls from the 
LLM and calls the appropriate tools via MCP.
"""
